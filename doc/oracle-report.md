# rust-hdf5 parity oracle — report

Generated by `oracle/run.py` against libhdf5 1.14.6 / h5py 3.15.1. 2 cases.

## How to run

```sh
cargo build --release --bin oracle_probe
$RUST_HDF5_ORACLE_PYTHON oracle/run.py        # or just: python3 oracle/run.py
```

`run.py` re-executes itself under `RUST_HDF5_ORACLE_PYTHON` (default `/home/stevek/micromamba/envs/tomo/bin/python`) when the invoking interpreter has no h5py, builds the probe if it is missing, and rewrites this file plus `oracle/report.json`. `--filter SUBSTR` restricts the matrix, `--keep` leaves the generated `.h5` files in the work directory.

## Verdicts

**Direction A** (h5py writes, rust-hdf5 reads) — `DIFF` at least one field where both sides produced a value and the values disagree; `MISS` no divergence, but an object that is in the file never appears in `group_names`/`dataset_names`, so the reader does not even report an error for it; `GAP` a field this file has that the public API cannot observe here (an unreadable datatype, an unresolvable link); `PASS` everything this file contains was read correctly; `READ-ERROR` the probe could not open or walk the file at all.

`PASS` tolerates the 13 accessors that are missing from the API *everywhere* (`attrorder`, `attrstore`, `chunkindex`, `external`, `fillvalue`, `filters`, `layout`, `linkorder`, `linkstore`, `maxshape`, `nattrs_hdr`, `superblock`, `virtual`) — they are counted once each in the findings table below rather than against every case that happens to contain a dataset.

**Direction B** (rust-hdf5 writes, h5py/libhdf5 reads) — `PASS` h5py read it, every core field (kind, dtype, shape, data, attributes, link targets) matched the reference and `h5diff`/`h5dump` were clean; `INVALID` one of those failed; `UNSUPPORTED-API` the public API cannot express the case. Differences confined to `attrorder`, `chunkindex`, `fillvalue`, `filters`, `layout`, `linkorder`, `linkstore`, `maxshape`, `superblock` are recorded as metadata deviations and do not fail a case, because the values libhdf5 reads are identical.

## Headline

| direction | PASS | GAP | MISS | DIFF | READ-ERROR | GEN-ERROR |
|---|---|---|---|---|---|---|
| A (read) | 2 | 0 | 0 | 0 | 0 | 0 |

| direction | PASS | INVALID | UNSUPPORTED-API | SKIPPED |
|---|---|---|---|---|
| B (write) | 2 | 0 | 0 | 0 |

## Top gaps by severity

| # | severity | finding | cases |
|---|---|---|---|
| 1 | no public accessor (API-wide) | attrorder — H5Group exposes no attribute creation-order tracking flags | 2 (link_external, link_external_read) |
| 2 | no public accessor (API-wide) | attrstore — H5Group exposes no compact/dense attribute storage accessor | 2 (link_external, link_external_read) |
| 3 | no public accessor (API-wide) | linkorder — H5Group exposes no link creation-order tracking flags | 2 (link_external, link_external_read) |
| 4 | no public accessor (API-wide) | linkstore — H5Group exposes no compact/dense link storage accessor | 2 (link_external, link_external_read) |
| 5 | no public accessor (API-wide) | nattrs_hdr — H5Group exposes no object-header attribute count | 2 (link_external, link_external_read) |
| 6 | no public accessor (API-wide) | superblock — H5File exposes no superblock/libver accessor | 2 (link_external, link_external_read) |
| 7 | no public accessor (API-wide) | attrstore — H5Dataset exposes no compact/dense attribute storage accessor | 1 (link_external) |
| 8 | no public accessor (API-wide) | external — H5Dataset exposes no external file list accessor | 1 (link_external) |
| 9 | no public accessor (API-wide) | fillvalue — H5Dataset exposes no fill value accessor | 1 (link_external) |
| 10 | no public accessor (API-wide) | filters — H5Dataset exposes no filter pipeline accessor | 1 (link_external) |

## Case matrix

| case | group | A | div | miss | gap | B | note |
|---|---|---|---|---|---|---|---|
| `link_external` | link | PASS | 0 | 0 | 0 | PASS |  |
| `link_external_read` | link | PASS | 0 | 0 | 0 | PASS |  |

## Direction A divergences, in full

None.
## Objects the reader does not list

None.

## Direction B expected deviations

The rust-written file carries the same data, type and shape as the h5py reference but describes itself differently. Each row below is a known, understood writer deviation declared in `EXPECTED_DEVIATIONS` (oracle/run.py); it does not fail a case. `observed: no` means a declared deviation no longer happens — either the writer was fixed and the entry should go, or the cases that exercised it changed.

| id | field | libhdf5 | rust-hdf5 | observed | cases |
|---|---|---|---|---|---|
| `superblock-floor-v2` | `superblock` | `0` | `2` | yes | 2 (link_external, link_external_read) |
| `superblock-v3-for-chunk-index` | `superblock` | `*` | `3` | no | 0 |
| `btree1-index-substituted` | `chunkindex` | `btree1` | `*` | no | 0 |
| `new-style-groups-always` | `linkstore` | `symtab` | `compact` | yes | 2 (link_external, link_external_read) |
| `filter-flags-zero` | `filters` | `*` | `*` | no | 0 |

- `superblock-floor-v2`: libhdf5 writes a v0 superblock at the earliest libver bound, over symbol-table groups and version-1 object headers; this writer emits neither, and no libhdf5 writes a v0 superblock over the link-message groups it does emit, so its floor is the v1.8 bound's version 2 (HDF5_superblock_ver_bounds); observed as `0 -> 2`
- `superblock-v3-for-chunk-index`: every chunked dataset this writer emits is indexed by a v1.10 structure through a version-4 data layout message, which H5O_layout_ver_bounds puts at the v1.10 bound and so at superblock version 3, whatever bound the file asked for
- `btree1-index-substituted`: a v1 B-tree chunk index is only legal below superblock v3, so this follows from superblock-v3-for-chunk-index: the file gets whichever v1.10 index its shape selects — the extensible array for one unlimited dimension, the single-chunk index for a fixed shape covered by one chunk
- `new-style-groups-always`: the writer emits a version-2 object header for every group, so links libhdf5 would keep in a symbol table are stored as link messages; the same cause as superblock-floor-v2, and the phase change past max_compact still moves them to dense storage; observed as `symtab -> compact`
- `filter-flags-zero`: the writer stores the per-filter flags byte as 0 where libhdf5 stores 1 (H5Z_FLAG_OPTIONAL); both pipelines decode to the same bytes

## Direction B unexpected deviations

None: every metadata deviation in this run is a declared one.

## Direction B failures, in full

None.
## All findings

| severity | finding | cases | example |
|---|---|---|---|
| no public accessor (API-wide) | attrorder — H5Group exposes no attribute creation-order tracking flags | 2 | /#attrorder |
| no public accessor (API-wide) | attrstore — H5Group exposes no compact/dense attribute storage accessor | 2 | /#attrstore |
| no public accessor (API-wide) | linkorder — H5Group exposes no link creation-order tracking flags | 2 | /#linkorder |
| no public accessor (API-wide) | linkstore — H5Group exposes no compact/dense link storage accessor | 2 | /#linkstore |
| no public accessor (API-wide) | nattrs_hdr — H5Group exposes no object-header attribute count | 2 | /#nattrs_hdr |
| no public accessor (API-wide) | superblock — H5File exposes no superblock/libver accessor | 2 | #superblock |
| no public accessor (API-wide) | attrstore — H5Dataset exposes no compact/dense attribute storage accessor | 1 | /orig#attrstore |
| no public accessor (API-wide) | external — H5Dataset exposes no external file list accessor | 1 | /orig#external |
| no public accessor (API-wide) | fillvalue — H5Dataset exposes no fill value accessor | 1 | /orig#fillvalue |
| no public accessor (API-wide) | filters — H5Dataset exposes no filter pipeline accessor | 1 | /orig#filters |
| no public accessor (API-wide) | layout — H5Dataset::is_chunked() is false | 1 | /orig#layout |
| no public accessor (API-wide) | maxshape — H5Dataset exposes no max_shape() accessor | 1 | /orig#maxshape |
| no public accessor (API-wide) | nattrs_hdr — H5Dataset exposes no object-header attribute count | 1 | /orig#nattrs_hdr |
| no public accessor (API-wide) | virtual — H5Dataset exposes no virtual mapping accessor | 1 | /orig#virtual |

## Known modelling gaps in the canonical format

- The string pad of a *variable-length* string is reported in the separate `strpad` field rather than inline in `dtype`; a fixed string keeps its pad inline. Both sides answer both forms — the split is a layout choice of this format, not a gap.
- `chunkindex` is derived on the libhdf5 side from the DCPL and the dataspace, following the library's own selection rules: neither h5py nor the h5 CLI tools report the stored index type.
- h5py 3.x exposes neither `get_offset` nor `get_precision` on a bitfield type, so a sub-width bitfield would be reported at full width. No case exercises that.

